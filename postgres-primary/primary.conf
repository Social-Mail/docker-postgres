# postgresql.conf (mounted into the container)
wal_level = replica
archive_mode = on
archive_command = 'mkdir -p /db/wal/archives && cp %p /db/wal/archives/%f' # Replace with your actual command
archive_timeout = 60 #